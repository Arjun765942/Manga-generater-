## 2025-02-18 - File Input Accessibility
**Learning:** `hidden` inputs are inaccessible to keyboard users; use `sr-only` + `focus-within` on parent label instead.
**Action:** Always use `sr-only` for file inputs and style the wrapping label with `focus-within:ring` or `focus-within:outline`.
